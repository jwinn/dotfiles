# shellcheck shell=sh

# WARNING: this will not be run for non-login, non-interactive shells.

# Also, you must run ${XDG_CONFIG_HOME}/shell/env, to get $ENV.
# shellcheck source-path=SCRIPTDIR
. "{{ .xdg.config.home }}/shell/env.sh"

