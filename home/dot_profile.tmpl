# shellcheck shell=sh

# WARNING: if you delete .bash_profile, this file becomes part of bash's
# startup sequence, which means this file suddenly has to cater for two
# different shells.

# shellcheck source-path=SCRIPTDIR
. "{{ .xdg.config.home }}/sh/env.sh"
# shellcheck source-path=SCRIPTDIR
. "{{ .xdg.config.home }}/sh/login.sh"

